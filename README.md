# Streamlit-RAG-OllamaApp
This project demonstrates a Retrieval-Augmented Generation (RAG) chatbot that answers questions about President Biden's 2023 State of the Union speech.  The application runs entirely locally using Ollama and uses a vector database to retrieve relevant context before generating responses.
